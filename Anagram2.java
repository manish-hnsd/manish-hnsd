public class Anagram2 
{
    public static void main(String[] args) {
        int a[] = new int[256];
        int b[] = new int[256];

        for(char c: a.toCharArray())
        {
            int index = (int) c;
            a[index]+=;
        }
        for(int i =0;i<256;i++)
        {
            if(a[i] != b[i])
            {
                isAnagram = false;
                break;
            }
        }
    }
    
}
